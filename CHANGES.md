# Changes

## 0.1.0 (2026-07-22)

- Initial Dune-based bindings to robotstxt v1.1.0.
- Expose allow/deny matching, multiple user-agents, matching metadata,
  Crawl-delay, Request-rate, Content-Signal, validation, and version queries.
- Declare the C++ compiler check needed for clean opam installation.
- Use a pinned Ada v3.4.4 build for upstream-equivalent WHATWG URL parsing and
  normalization.
